package com.turtlesafety.radar

import android.app.Application

class RadarApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        // Module wiring is performed in Phase 1 (Core MVP).
    }
}
